import 'package:flutter/material.dart';

class PAMShowImage extends StatelessWidget {
  const PAMShowImage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}